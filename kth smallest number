def kth_smallest(arr, k):

    sorted_arr = sorted(arr)
    
    return sorted_arr[k - 1]  # Adjusting for 0-based indexing

arr = [7, 10, 4, 3, 20, 15]
k = 3
print(f"The {k}th smallest number is: {kth_smallest(arr, k)}")
