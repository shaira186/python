def count_occurrences(s, char):
    count = 0
    for c in s:
        if c == char:
            count += 1
    return count


s = "PYTHON CODE"
char = "O"
occurrences = count_occurrences(s, char)
print(f"The character '{char}' appears {occurrences} times in the string.")
